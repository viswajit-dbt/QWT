version: 2

sources:
  - name: raw_qwt
    database: "{{env_var('DBT_SOURCE_DB','QWT_ANALYTICS_DEV')}}"
    schema: "{{env_var('DBT_RAW_SCH','RAW_DEV')}}"
    tables:
      - name: customers
      - name: raw_products
      - name: raw_orders
      - name: raw_orderdetails
      - name: raw_employees
      - name: raw_offices
      - name: raw_shipments